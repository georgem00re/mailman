
interface KeyValuePair {
	selected?: boolean;
	keyStr: string;
	value: string;
}

export interface RequestHeader {
	selected?: boolean,
	keyStr: string,
	value: string,
}

export interface QueryParam {
	selected?: boolean,
	keyStr: string,
	value: string,
}