
interface KeyValuePair {
	selected?: boolean;
	keyStr: string;
	value: string;
}

export interface RequestHeader {
	selected?: boolean,
	key: string,
	value: string,
}

export interface QueryParam {
	selected?: boolean,
	key: string,
	value: string,
}